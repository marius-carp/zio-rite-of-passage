package com.frunza.reviewboard.config

case class EmailServiceConfig(host: String, 
                              port: Int,
                              user: String,
                              pass: String)
